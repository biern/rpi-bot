{
  "name": "rpi-bot",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && cp package.json dist/"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pigpio": "^1.1.0"
  },
  "devDependencies": {
    "@types/pigpio": "^0.4.1",
    "typescript": "^2.9.1"
  }
}
